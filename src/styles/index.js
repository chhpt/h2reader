import Colors from './Colors';
import Metrics from './Metrics';


const Styles = {
  tabBar: {
    flex: 1,
    height: 40,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center'
  }
};

export { Styles };
